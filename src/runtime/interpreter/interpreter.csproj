<Project Sdk="Microsoft.NET.Sdk">
    <PropertyGroup>
        <AssemblyName>Vezel.Celerity.Runtime.Interpreter</AssemblyName>
        <IsPackable>true</IsPackable>
        <PackageDescription>$(Description)

This package provides the portable bytecode interpreter which does not require
dynamic code generation.</PackageDescription>
        <PackageId>Vezel.Celerity.Runtime.Interpreter</PackageId>
        <RootNamespace>Vezel.Celerity.Runtime.Interpreter</RootNamespace>
    </PropertyGroup>

    <ItemGroup>
        <ProjectReference Include="../core/core.csproj" />
    </ItemGroup>

    <Import Project="../../language/core/core.targets" />
    <Import Project="../core/core.targets" />
</Project>
